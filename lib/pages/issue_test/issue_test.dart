import 'package:flutter/material.dart';

class IssueTest extends StatelessWidget {
  const IssueTest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('테스트');
  }
}
